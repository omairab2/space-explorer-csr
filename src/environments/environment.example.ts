// Template for environment.ts (which is gitignored so the key never gets committed).
// Copy this file to `environment.ts` in the same folder and set your own key:
//   - Get a free NASA API key at https://api.nasa.gov
//   - cp src/environments/environment.example.ts src/environments/environment.ts
export const environment = {
  production: false,
  nasaApiKey: 'YOUR_NASA_API_KEY_HERE',
};
